# Conditionals and Control Flow | Codecademy

# Quiz: Conditionals and Control Flow

## Questions

The `break` keyword is important in a `switch` statement due to which of the following?

## Answer Choices

-   It is necessary whenever the `switch` statement contains more than 4 cases.
-   Java will return an error if it is not specified.
-   (Selected)Correct:

    Without `break`, Java will continue to check other cases in the `switch` statement.

    👏

    Correct!

-   Without it, the `switch` statement will not execute.

## Embedded Content

---

# Conditionals and Control Flow | Codecademy

# Quiz: Conditionals and Control Flow

## Questions

What will the following code print?

### Code

```
if (5 > 6) {    System.out.println("C. Senor");} else if (6 < 5) {    System.out.println("Sue Yu");} else {    System.out.println("Jim Sox");}
```

## Answer Choices

-   (Selected)Correct:

    `Jim Sox`

    👏

    Correct! `5 > 6` returns `false` and also `6 < 5` also returns `false`. So the `else` statement is ran.

-   A blank line.
-   `C. Senor`
-   `Sue Yu`

## Embedded Content

---

# Conditionals and Control Flow | Codecademy

# Quiz: Conditionals and Control Flow

## Questions

The AND logical operator is represented in Java by which operator?

## Answer Choices

-   `!`
-   `||`
-   (Selected)Correct:

    `&&`

    👏

    Correct!

-   `and`

## Embedded Content

---

# Conditionals and Control Flow | Codecademy

# Quiz: Conditionals and Control Flow

## Questions

The following Boolean expression will be evaluated to what value?

### Code

```
!(false) && (5 > 4)
```

## Answer Choices

-   `false`
-   `unknown`
-   (Selected)Correct:

    `true`

    👏

    Correct! `!(false)` returns `true` and `( 5 > 4 )` is also true. `true && true` is `true`!

-   `( 5 > 4 )`

## Embedded Content

---

# Conditionals and Control Flow | Codecademy

# Quiz: Conditionals and Control Flow

## Questions

The OR logical operator is represented in Java by which operator?

## Answer Choices

-   `!`
-   `&&`
-   `OR`
-   (Selected)Correct:

    `||`

    👏

    Correct!

## Embedded Content

---

# Conditionals and Control Flow | Codecademy

# Quiz: Conditionals and Control Flow

## Questions

If multiple conditional operators are placed in a single expression, which operator gets evaluated first?

## Answer Choices

-   The AND operator - `&&`
-   (Selected)Correct:

    The `NOT` operator - `!`

    👏

    Correct! The order of evaluation is: the NOT operator (`!`) -> the AND operator (`&&`) -> the OR operator (`||`)

-   An expression cannot contain more than one conditional operator.
-   The `OR` operator - `||`

## Embedded Content

---

# Conditionals and Control Flow | Codecademy

# Quiz: Conditionals and Control Flow

## Questions

What will the following code print?

### Code

```
int choice = 5;switch (choice) {  case 4:     System.out.println("Jo Fraizer");    break;  case 5:     System.out.println("Laila A. Lee");    break;  case 6:     System.out.println("Hall E. Home");    break;  default:     System.out.println(200);}
```

## Answer Choices

-   `Jo Fraizer`
-   (Selected)Correct:

    `Laila A. Lee`

    👏

    Correct! `choice` is `5` so `case 5`‘s code block will run.

-   `Hall E. Home`
-   `200`

## Embedded Content

---
