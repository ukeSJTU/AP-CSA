package exercises.starter;

public class ReservationValidation {
    private int guestCount;

    public ReservationValidation(int count) {
        // TODO: Write a conditional using OR operator to check if count is less than 1
        // OR greater than 8
        // - If true, print "Invalid reservation!"
        guestCount = count;
    }
}
