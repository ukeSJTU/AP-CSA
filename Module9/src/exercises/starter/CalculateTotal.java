import java.util.ArrayList;

public class CalculateTotal {
    public static void main(String[] args) {
        ArrayList<Double> expenses = new ArrayList<Double>();
        expenses.add(74.46);
        expenses.add(63.99);
        expenses.add(10.57);
        expenses.add(81.37);

        double total = 0;

        // TODO: Use a for loop to iterate over expenses and sum up the total
        // Initialize a counter i at 0, run while i is less than the size() of expenses,
        // and increment i
        // Inside the loop, add the item's value to total

        System.out.println("Total expenses: " + total);
    }
}
