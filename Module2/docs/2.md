# Variables | Codecademy

# Quiz: Java Variables Quiz

## Questions

Which one of the following values is a valid `char`?

## Answer Choices

-   “a”
-   ‘ab’
-   7
-   (Selected)Correct:

    ‘F’

    👏

    Yes! This is a single character within single quotes.

# Variables | Codecademy

# Quiz: Java Variables Quiz

## Questions

What is the value of `num`?

### Code

```
int num = (10 - (4 + 3)) * 6;
```

## Answer Choices

-   `-32`
-   (Selected)Correct:

    `18`

    👏

    Correct! Using the order of operations, `(4 + 3)` is evaluated first. Then, `(10 - 7)` is evaluated. Finally, `3 * 6` is evaluated producing a value of `18`.

-   `24`
-   `20`

# Variables | Codecademy

# Quiz: Java Variables Quiz

## Questions

What value CANNOT be assigned to a variable with the datatype `double`.

## Answer Choices

-   (Selected)Correct:

    “60”

    👏

    Correct! Since there are double quotes, this is a String instead of a `double`

-   \-.2
-   6.7
-   5

# Variables | Codecademy

# Quiz: Java Variables Quiz

## Questions

What line of code declares a variable called `numRabbits` to store a whole number?

## Answer Choices

-   `number numRabbits;`
-   (Selected)Correct:

    `int numRabbits;`

    👏

    Correct! The `int` type stores whole numbers!

-   `numRabbits int;`
-   `numRabbits = int;`

# Variables | Codecademy

# Quiz: Java Variables Quiz

## Questions

Which line declares the variable `bestProgrammingLanguage` and initializes it to be `"Java"`?

## Answer Choices

-   (Selected)Correct:

    `String bestProgrammingLanguage = "Java";`

    👏

    Correct! This declares the type `String` and initializes the value to be `"Java"`

-   `"Java" = String bestProgrammingLanguage;`
-   `bestProgrammingLanguage = String "Java";`
-   `string bestProgrammingLanguage = "Java";`

# Variables | Codecademy

# Quiz: Java Variables Quiz

## Questions

What datatype can only be assigned one of two values?

## Answer Choices

-   (Selected)Correct:

    `boolean`

    👏

    Yes! `boolean` variables can only either hold `true` or `false`.

-   `int`
-   `char`
-   `double`

# Variables | Codecademy

# Quiz: Java Variables Quiz

## Questions

Which of the following lines would throw a compilation error?

## Answer Choices

-   `String gradeOnTest = "A";`
-   `char grade_on_test = 'F';`
-   (Selected)Correct:

    `double isRaining = false;`

    👏

    Correct! The variable is declared to be of type `double` but initialized to `false`, which is a boolean.

-   `int balance = -30;`

# Variables | Codecademy

# Quiz: Java Variables Quiz

## Questions

Which option is a valid variable name and follows the Java naming conventions?

## Answer Choices

-   `2ndPhoneNumber`
-   `TimeUntilLaunch`
-   (Selected)Correct:

    `timeUntilLaunch`

    👏

    Yes! The first letter is uncapitalized, there are no underscores, and each non-starting word is capitalized.

-   `second_phone_number`
