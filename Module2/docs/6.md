# Variables | Codecademy

# Quiz: Java Variable Manipulation Quiz

## Questions

The expression `5 != 6` will evaluate to what value?

## Answer Choices

-   `6`
-   `false`
-   `5`
-   (Selected)Correct:

    `true`

    👏

    Correct! Since `5` is not equal to `6`, this line evaluates to `true`.

# Variables | Codecademy

# Quiz: Java Variable Manipulation Quiz

## Questions

What will the following program output?

### Code

```
int num = 12;num *= 2;num -= 4;num++;System.out.println(num);
```

## Answer Choices

-   `20`
-   `9`
-   `12`
-   (Selected)Correct:

    `21`

    👏

    Correct! `num` was declared with a value of `12`. Then, `num` was multiplied by `2`. Next, `4` was subtracted from `num`. Finally, `num` was increased by `1`…

    Show more

# Variables | Codecademy

# Quiz: Java Variable Manipulation Quiz

## Questions

To what value does the following string concatenation evaluate?

### Code

```
"It's " + 5 + "pm"
```

## Answer Choices

-   Error
-   (Selected)Correct:

    `"It's 5pm"`

    👏

    Correct! The `+` will turn `5` into a String and then concatenate it.

-   `"It's pm"`
-   11

# Variables | Codecademy

# Quiz: Java Variable Manipulation Quiz

## Questions

After the following code is run, what value will the variable `endpoint` be assigned to?

### Code

```
int endpoint = 11 % 3;
```

## Answer Choices

-   (Selected)Correct:

    2

    👏

    Yes! The modulo operator `%` returns the remainder. The remainder of `11` divided by `3` is `2`.

-   11
-   2.66
-   1

# Variables | Codecademy

# Quiz: Java Variable Manipulation Quiz

## Questions

What is the best way to tell if the following two Strings are equal?

### Code

```
String username1 = "teracoder";String username2 = "gigacoder";
```

## Answer Choices

-   `username1.isEqualTo(username2)`
-   `System.out.println(username1)`
-   `username1==username2`
-   (Selected)Correct:

    `username1.equals(username2)`

    👏

    Correct! This command will compare the usernames against each other.

# Variables | Codecademy

# Quiz: Java Variable Manipulation Quiz

## Questions

What does the following code do?

### Code

```
System.out.println(8 <= 8);
```

## Answer Choices

-   Prints `8`.
-   (Selected)Correct:

    Prints `true`.

    👏

    Correct! 8 is less than or equal to 8, so the code inside the `System.out.println()` evaluates to `true`.

-   Prints `false`.
-   Prints `0`.

# Variables | Codecademy

# Quiz: Java Variable Manipulation Quiz

## Questions

Are there any errors in this Java statement?

### Code

```
int status = 7 < 8;
```

## Answer Choices

-   There are no errors.
-   (Selected)Correct:

    Yes, `int` should be `boolean`.

    👏

    Yes! The expression `7 < 8` will evaluate to `true`, which must be stored as a `boolean`, not an `int`.

-   Yes, there should be no semicolon.
-   Yes, `int` should be `char`.

# Variables | Codecademy

# Quiz: Java Variable Manipulation Quiz

## Questions

How could we get a result of `10`, given the following variable?

### Code

```
double a = 2;
```

## Answer Choices

-   `a - 12`
-   `a % 10`
-   `a / 2`
-   (Selected)Correct:

    `a * 5`

    👏

    Yes! `2` multiplied by `5` will give us `10`.

# Variables | Codecademy

# Quiz: Java Variable Manipulation Quiz

## Questions

Which operator can be used to concatenate two Strings?

## Answer Choices

-   `*`
-   `-`
-   (Selected)Correct:

    `+`

    👏

    Yes! The plus sign will create a new combined String from two Strings.

-   `.equals()`

# Variables | Codecademy

# Quiz: Java Variable Manipulation Quiz

## Questions

True or False: The value of a variable declared with the `final` keyword can be changed after its initial declaration.

## Answer Choices

-   True
-   (Selected)Correct:

    False

    👏

    Correct! If a variable is declared with the `final` keyword, its value cannot be changed.
