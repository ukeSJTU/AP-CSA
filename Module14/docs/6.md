# Searching and Sorting | Codecademy

# Quiz: Selection Sort: Java

## Questions

Fill in the correct code for the inner for loop for selection sort.

### Code

```
public static void selectionSort (int arr[]) {  int size = arr.length;  for (int i = 0; i < size - 1; i++) {    int currentMinimumIndex = i;    for (int j = i + 1; j < arr.length; j++) {      if (arr[j] < arr[currentMinimumIndex]) {        currentMinimumIndex = j;      }    }    int temp = arr[currentMinimumIndex];    arr[currentMinimumIndex] = arr[i];    arr[i] = temp;  }}
```

## Answer Choices

Click or drag and drop to fill in the blank

👏

You got it!

## Embedded Content

# Searching and Sorting | Codecademy

# Quiz: Selection Sort: Java

## Questions

What are the correct intermediate steps of the following data set when it is being sorted with the Selection sort? `[15, 12, 40, -3]`

## Answer Choices

-   `[15, 12, -3, 40]` -> `[-3, 15, 12, 40]` -> `[-3, 12, 15, 40]`
-   `[-3, 15, 12, 40]` -> `[-3, 12, 15, 40]`
-   (Selected)Correct:

    `[-3, 12, 40, 15]` -> `[-3, 12, 40, 15]` -> `[-3, 12, 15, 40]`

    👏

    Correct!

-   `[12, 15, 40, 3]` -> `[12, 15, 3, 40]` -> `[12, 3, 15, 40]` -> `[3, 12, 15, 40]`

## Embedded Content

# Searching and Sorting | Codecademy

# Quiz: Selection Sort: Java

## Questions

Selection sort is the most efficient of the comparison sorting algorithms.

## Answer Choices

-   (Selected)Correct:

    False.

    👏

    Correct! Selection sort is a good introduction to algorithmic thinking and sorting in particular, but it is not the most efficient due to the nested loop str…

    Show more

-   True.

## Embedded Content

# Searching and Sorting | Codecademy

# Quiz: Selection Sort: Java

## Questions

Which of the following blocks of code successfully swaps two elements at `indexOne` and `indexTwo` in an array called `arr`?

## Answer Choices

-   ```
    int indexTwo = indexOne;arr[indexTwo] = arr[indexOne];arr[indexOne] = arr[indexTwo];
    ```
-   (Selected)Correct:

    ```
    int temp = arr[indexTwo];arr[indexTwo] = arr[IndexOne];arr[indexOne] = temp;
    ```

    👏

    Well done! We need a placeholder variable, in this case `temp`, to store the value of one of the elements so we do not lose the value when we change the valu…

    Show more

-   ```
    int temp = arr[indexOne];arr[indexTwo] = arr[indexOne];arr[indexOne] = temp;
    ```

## Embedded Content

# Searching and Sorting | Codecademy

# Quiz: Selection Sort: Java

## Questions

An input list which is mostly ordered will require less comparisons than an input list which is unordered.

## Answer Choices

-   True.
-   (Selected)Correct:

    False.

    👏

    Correct! Even if the list is already sorted, the inner for loop will have to make `n` comparisons to find the smallest remaining unsorted item.

## Embedded Content

# Searching and Sorting | Codecademy

# Quiz: Selection Sort: Java

## Questions

Fill in the missing code.

### Code

```
public static void selectionSort (int arr[]) {    int size = arr.length;    for (int i = 0; i < size - 1; i++) {      int currentMinimumIndex = i;      for (int j = i + 1; j < arr.length; j++) {        if (arr[j] < arr[currentMinimumIndex]) {          currentMinimumIndex = j;        }      }      int temp = arr[currentMinimumIndex];      arr[currentMinimumIndex] = arr[i];      arr[i] = temp;}
```

## Answer Choices

-   size
-   j

Click or drag and drop to fill in the blank

👏

You got it!

## Embedded Content

# Searching and Sorting | Codecademy

# Quiz: Selection Sort: Java

## Questions

Fill in the missing code needed to swap elements.

### Code

```
public static void selectionSort (int arr[]) {    int size = arr.length;    for (int i = 0; i < size - 1; i++) {      int currentMinimumIndex = i;      for (int j = i + 1; j < arr.length; j++) {        if (arr[j] < arr[currentMinimumIndex]) {          currentMinimumIndex = j;        }      }      int temp = arr[currentMinimumIndex];      arr[currentMinimumIndex] = arr[i];      arr[i] = temp;}
```

## Answer Choices

Click or drag and drop to fill in the blank

👏

You got it!

## Embedded Content

# Searching and Sorting | Codecademy

# Quiz: Selection Sort: Java

## Questions

What is the Big O runtime of selection sort?

## Answer Choices

-   Exponential - `O(2^N)`
-   Linear - `O(N)`
-   Constant - `O(1)`
-   (Selected)Correct:

    Quadratic - `O(N^2)`

    👏

    Correct! Selection sort is implemented with nested loops that iterate through the elements of the input array which results in a quadratic runtime.

## Embedded Content
